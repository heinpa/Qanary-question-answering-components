version: '3'
services: 
  component:
    # for building from source
    image: heipa/qanary-component-mbart:thesis
    build: 
      context: .
      dockerfile: Dockerfile
    network_mode: host
    env_file:
      - .env
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface/
